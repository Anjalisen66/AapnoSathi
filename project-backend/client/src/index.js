import React from "react";
import ReactDOM from "react-dom/client";
import App from "./app"; // This is now the main `App.js`

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
